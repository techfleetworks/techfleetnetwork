# Control Matrix — SPF Data Layer + Hand-Off System

The single most useful audit artifact: control → requirement → implementation → where the
evidence lives. Reuses already-audited platform controls (SSO, secrets, deploy records,
logging) where possible. Rows marked _(planned)_ land in the phase noted.

| Control                          | Requirement (skill)            | Implementation                                                                                                                  | Evidence                                                              |
| -------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Access control / least privilege | SOC2 CC6; OWASP access-control | RLS: `active_participant` on own project to produce; any active member uploads; `has_role` admin; deny-by-default               | RLS policies in migration; `@security` IDOR scenarios _(Phase B1)_    |
| Signed-URL scoping               | OWASP access-control           | short-lived, project-scoped, ownership re-checked                                                                               | `createSignedUrl` call site; `@security` scenario _(Phase B3)_        |
| Encryption in transit + at rest  | SOC2 CC6.7; privacy            | TLS to SPF/Groq (cert-validate); Supabase at-rest for DB + private buckets                                                      | infra config; ADR-0002                                                |
| Change management                | SOC2 CC8; release-safety       | expand/contract migrations; deployment record (SHA, flags, migrations); ADRs                                                    | git history; `docs/adr/`; deploy log _(Phase A)_                      |
| Audit logging (tamper-evident)   | compliance audit-logging       | append-only log of produce/download/regenerate, SPF sync/replace, cross-project access, DSAR, retention jobs — ids not contents | audit table + `@compliance` scenarios _(Phase A2/B3)_                 |
| Data classification + inventory  | compliance data-classification | every store classified Restricted/Confidential/Public                                                                           | `spf-handoff-data-classification.md`                                  |
| Retention + deletion propagation | GDPR/CCPA erasure              | automated expiry (soft→hard); DSAR deletion reaches blobs+versions+fact base+vectors                                            | data-classification doc; `@compliance` deletion scenario _(Phase B3)_ |
| Data minimization                | GDPR minimization              | workshops/deliverables thin-projection; ≤10k text cap                                                                           | ADR/plan; `spf-handoff-data-classification.md`                        |
| DPIA                             | GDPR high-risk/new-tech        | AI-processing risk assessment                                                                                                   | `spf-handoff-dpia.md`                                                 |
| Subprocessor management          | SOC2 CC9.2; GDPR               | Groq DPA + subprocessor register + residency assessment                                                                         | _(owner action — flagged)_                                            |
| Vulnerability mgmt / SCA         | OWASP third-party              | npm/pip audit + Trivy + IaC scan in CI                                                                                          | `security.yml` / `pentest.yml` additions _(Phase 0/A)_                |
| Secrets management               | OWASP crypto-secrets           | edge secret store; fail-fast; gitleaks; least-privilege Figma scope                                                             | secret config; `@security` secret scenario                            |
| Backup + DR (RPO/RTO)            | compliance safe-migrations     | verified restorable backup before destructive drop; restore drill; RPO/RTO defined                                              | `spf-handoff-slos.md`; restore-drill record _(Phase A4)_              |
| Migration integrity              | compliance safe-migrations     | node/edge/row parity self-check before+after; **abort+rollback on failure**                                                     | `@data-lifecycle` migration scenario _(Phase A2)_                     |
| Reliability (SLO/error budget)   | SRE                            | freshness + produce SLOs; golden signals; burn-rate alerts                                                                      | `spf-handoff-slos.md`; dashboards _(Phase A/B)_                       |
| Lockout prevention               | OWASP Step 0                   | additive-before-subtractive; preserve admin/owner; explicit confirm for drop                                                    | `@lockout-prevention` scenarios; ADR-0003                             |
