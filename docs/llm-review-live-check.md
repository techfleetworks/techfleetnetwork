# llm-arch-review — live check

Throwaway file used to confirm the `llm-arch-review` CI Action runs against a real PR and posts its
`🏛️ Architecture review (advisory)` comment end-to-end (real model call + comment upsert). This PR
is not meant to be merged — it is closed after the comment is confirmed.
