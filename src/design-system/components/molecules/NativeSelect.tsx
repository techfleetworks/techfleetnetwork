/**
 * NativeSelect (molecule) — a select backed by the native <select> element
 * (best for long option lists and mobile pickers). MUI NativeSelect.
 * For the styled popup select prefer the `Select` molecule.
 * See docs/design/design-system/components/molecules/NativeSelect.md
 */
export { default as NativeSelect } from "@mui/material/NativeSelect";
export type { NativeSelectProps } from "@mui/material/NativeSelect";
