/**
 * TextField (molecule) — MUI's all-in-one labeled text field (label + input +
 * helper text in one component), built on the themed OutlinedInput.
 *
 * NOTE: the TFDS default for forms is the `Field` + `Input`/`RHFTextField`
 * composition (explicit label + helper accessibility). Raw TextField is exported
 * full MUI parity and quick one-off fields.
 * See docs/design/design-system/components/molecules/TextField.md
 */
export { default as TextField } from "@mui/material/TextField";
export type { TextFieldProps } from "@mui/material/TextField";
