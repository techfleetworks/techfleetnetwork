/**
 * Popper (primitive) — positions a floating element next to an anchor (no
 * backdrop, no focus trap). MUI Popper. The building block under Tooltip,
 * Popover, and Autocomplete. Prefer `Popover`/`Tooltip` for app UI.
 * See docs/design/design-system/components/primitives/Popper.md
 */
export { default as Popper } from "@mui/material/Popper";
export type { PopperProps } from "@mui/material/Popper";
