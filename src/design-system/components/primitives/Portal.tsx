/**
 * Portal (primitive) — renders children into a different DOM node (e.g. document
 * body) while keeping them in the React tree. MUI Portal.
 * See docs/design/design-system/components/primitives/Portal.md
 */
export { default as Portal } from "@mui/material/Portal";
export type { PortalProps } from "@mui/material/Portal";
