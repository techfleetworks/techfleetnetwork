/**
 * Modal (primitive) — the low-level accessible overlay (focus trap + backdrop)
 * that Dialog/Drawer/Menu build on. MUI Modal.
 * Prefer the higher-level `Dialog`, `Sheet`, or `Drawer` for app UI; use Modal
 * directly only for fully custom overlays.
 * See docs/design/design-system/components/primitives/Modal.md
 */
export { default as Modal } from "@mui/material/Modal";
export type { ModalProps } from "@mui/material/Modal";
