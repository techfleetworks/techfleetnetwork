/**
 * TextareaAutosize (primitive) — an unstyled <textarea> that auto-grows to fit
 * its content. MUI TextareaAutosize. The styled DS textarea is `Textarea`; this
 * is the raw autosizing primitive (used by CharCountTextarea).
 * See docs/design/design-system/components/primitives/TextareaAutosize.md
 */
export { default as TextareaAutosize } from "@mui/material/TextareaAutosize";
export type { TextareaAutosizeProps } from "@mui/material/TextareaAutosize";
