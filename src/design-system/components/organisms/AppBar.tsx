/**
 * AppBar + Toolbar (organism) — the top application bar surface.
 * MUI AppBar (themed flat: brand paper background, bottom divider, no elevation
 * gradient — see MuiAppBar override) plus Toolbar for its inner layout row.
 * See docs/design/design-system/components/organisms/AppBar.md
 */
export { default as AppBar } from "@mui/material/AppBar";
export type { AppBarProps } from "@mui/material/AppBar";
export { default as Toolbar } from "@mui/material/Toolbar";
export type { ToolbarProps } from "@mui/material/Toolbar";
