/**
 * Command (organism) — command palette. KEEP-LIB: cmdk (MUI has no equivalent).
 * Re-exported unchanged so it's importable from @/design-system. Interim: the
 * underlying wrapper still lives in @/components/ui/command until teardown
 * rebuilds it directly on cmdk with DS theming.
 * See docs/design/design-system/components/organisms/Command.md
 */
export * from "@/components/ui/command";
