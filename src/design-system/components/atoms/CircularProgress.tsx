/**
 * CircularProgress (atom) — the circular/spinner progress indicator.
 * MUI CircularProgress. Complements `Progress` (the linear bar). Omit `value`
 * for an indeterminate spinner; pass `value` (0–100) + `variant="determinate"`
 * for a ring gauge. `Spinner` is exported as a friendly alias.
 * See docs/design/design-system/components/atoms/CircularProgress.md
 */
export { default as CircularProgress } from "@mui/material/CircularProgress";
export { default as Spinner } from "@mui/material/CircularProgress";
export type { CircularProgressProps } from "@mui/material/CircularProgress";
