/**
 * Link (atom) — a styled anchor. MUI Link, themed to the brand primary color
 * with a hover underline (see MuiLink in theme/components.ts). For in-app
 * navigation, pass `component={RouterLink}` so routing stays client-side.
 * See docs/design/design-system/components/atoms/Link.md
 */
export { default as Link } from "@mui/material/Link";
export type { LinkProps } from "@mui/material/Link";
