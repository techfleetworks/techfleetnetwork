/**
 * IconButton (atom) — a button whose content is a single icon.
 * MUI IconButton. Inherits the brand focus-visible ring from MuiButtonBase.
 * Pair with an accessible name (`aria-label`) — icon-only controls need it.
 * See docs/design/design-system/components/atoms/IconButton.md
 */
export { default as IconButton } from "@mui/material/IconButton";
export type { IconButtonProps } from "@mui/material/IconButton";
