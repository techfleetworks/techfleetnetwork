/**
 * Box (atom) — the low-level layout primitive with the `sx` styling prop.
 * MUI Box. Use for one-off spacing/layout; prefer Stack/Grid/Container for
 * structured layout on the 4px grid.
 * See docs/design/design-system/components/atoms/Box.md
 */
export { default as Box } from "@mui/material/Box";
export type { BoxProps } from "@mui/material/Box";
