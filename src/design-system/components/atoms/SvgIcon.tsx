/**
 * SvgIcon (atom) — wraps a custom SVG path as a theme-aware icon (inherits
 * `fontSize`/`color` like the Material icons do). MUI SvgIcon. Use for brand or
 * one-off glyphs not in @mui/icons-material; use the `Icon` atom for the latter.
 * See docs/design/design-system/components/atoms/SvgIcon.md
 */
export { default as SvgIcon } from "@mui/material/SvgIcon";
export type { SvgIconProps } from "@mui/material/SvgIcon";
