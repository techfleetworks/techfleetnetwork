/**
 * Chip (atom) — compact element for tags, filters, or entered values.
 * MUI Chip, themed via the TFDS theme (radius + palette). New in the catalog-
 * completion phase (no shadcn equivalent; brought in for full MUI Core parity).
 * See docs/design/design-system/components/atoms/Chip.md
 */
export { default as Chip } from "@mui/material/Chip";
export type { ChipProps } from "@mui/material/Chip";
