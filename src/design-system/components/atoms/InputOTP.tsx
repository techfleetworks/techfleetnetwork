/**
 * InputOTP (atom) — one-time-code input. KEEP-LIB: input-otp (MUI has no OTP
 * field). Re-exported so it's importable from @/design-system. Interim wrapper
 * lives in @/components/ui/input-otp until teardown rebuilds it on input-otp
 * directly with DS theming.
 * See docs/design/design-system/components/atoms/InputOTP.md
 */
export * from "@/components/ui/input-otp";
