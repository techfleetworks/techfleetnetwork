/**
 * Radio (atom) — a single radio control. MUI Radio, brand-colored to primary.
 * For grouped single-choice selection prefer the `RadioGroup` molecule, which
 * wires names + keyboard roving for you; use raw Radio only for custom layouts.
 * See docs/design/design-system/components/atoms/Radio.md
 */
export { default as Radio } from "@mui/material/Radio";
export type { RadioProps } from "@mui/material/Radio";
