// AUTO-GENERATED by scripts/generate-smoke-tests.ts — do not hand-edit.
// Smoke-tier coverage for BDD feature area: Discord Slash Command
import { describe, it, expect } from "vitest";
import fs from "fs";
import path from "path";

const appSrc = fs.readFileSync(path.join(process.cwd(), "src", "App.tsx"), "utf8");
const scenarioIds = ["DISC-SLASH-004"];

describe("Discord Slash Command" + " (smoke)", () => {
  it("DISC-SLASH-004: AI response is truncated for Discord message limit", () => {
    expect(appSrc).toMatch("/fleety");
  });
});
