// AUTO-GENERATED by scripts/generate-smoke-tests.ts — do not hand-edit.
// Smoke-tier coverage for BDD feature area: Admin Knowledge Ingest
import { describe, it, expect } from "vitest";
import fs from "fs";
import path from "path";

const appSrc = fs.readFileSync(path.join(process.cwd(), "src", "App.tsx"), "utf8");
const scenarioIds = ["55.4"];

describe("Admin Knowledge Ingest" + " (smoke)", () => {
  it("55.4: Non-admin cannot access ingest page", () => {
    expect(appSrc).toMatch("/admin/ingest");
  });
});
